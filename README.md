#Iris Classifier(Decision Tree)

##Overview

End-to-end ML example from Digital Marketing Mastery Module->builds a decision-tree classifier on the classic iris dataset using
sciket-learn.

##Quick start

```bash
git clone https://github.com/iris-classifier.git
cd iris-classifier
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python src/train.py
